// Splitter/Merger items removed — logistics now uses drone routes.
// Config stubs keep any remaining import references from failing.
export const SPLITTER_CONFIG = { workArea: { width: 4, depth: 4, color: '#f59e0b', opacity: 0.2 }, badgeHeight: 3.5 };
export const MERGER_CONFIG   = { workArea: { width: 4, depth: 4, color: '#8b5cf6', opacity: 0.2 }, badgeHeight: 3.5 };
