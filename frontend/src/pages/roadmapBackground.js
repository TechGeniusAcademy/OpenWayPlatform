// ════════════════════════════════════════════════════════════════════════════
//  КОНФИГ ФОНОВЫХ ОБЪЕКТОВ ROADMAP
//  Все 3D-объекты на заднем плане описываются здесь.
//  Компонент BackgroundObjects в Roadmap.jsx читает этот массив автоматически.
// ════════════════════════════════════════════════════════════════════════════
//
//  Поля:
//  ──────────────────────────────────────────────────────────────────────────
//  id        — уникальный идентификатор
//  file      — путь к GLB файлу (относительно /public)
//  pos       — [X, Y, Z]  позиция в сцене
//  scale     — число или [X, Y, Z]  масштаб
//  rotation  — начальный поворот [X, Y, Z] в радианах
//  spin      — { x, y, z }  скорость вращения в секунду (радиан/сек)
//              пример: { y: 0.003 } — медленно вращается по Y
//  opacity   — прозрачность от 0 до 1 (по умолчанию 1)
//
// ════════════════════════════════════════════════════════════════════════════

const BG_OBJECTS = [
  {
    id: "planet_phoenix",
    file: "/roadmap-models/planet_of_phoenix.glb",
    pos: [22, -6, -60],
    scale: 4.5,
    rotation: [0.1, 0, 0],
    spin: { y: 0.0003 },
    opacity: 1,
  },

  // Добавь следующий объект по аналогии:
  // {
  //   id: "asteroid_01",
  //   file: "/roadmap-models/asteroid.glb",
  //   pos: [-30, 12, -15],
  //   scale: 2,
  //   rotation: [0, 0, 0.4],
  //   spin: { x: 0.005, y: 0.002 },
  //   opacity: 0.85,
  // },
];

export default BG_OBJECTS;
